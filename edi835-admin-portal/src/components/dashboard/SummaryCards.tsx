import React from 'react';
import { Box } from '@mui/material';

interface SummaryCardsProps {
  [key: string]: any;
}

const SummaryCards: React.FC<SummaryCardsProps> = (props) => {
  return <Box>SummaryCards Component</Box>;
};

export default SummaryCards;
