import React from 'react';
import { Box } from '@mui/material';

interface ActiveBucketsWidgetProps {
  [key: string]: any;
}

const ActiveBucketsWidget: React.FC<ActiveBucketsWidgetProps> = (props) => {
  return <Box>ActiveBucketsWidget Component</Box>;
};

export default ActiveBucketsWidget;
