import React from 'react';
import { Box } from '@mui/material';

interface RejectionAnalyticsProps {
  [key: string]: any;
}

const RejectionAnalytics: React.FC<RejectionAnalyticsProps> = (props) => {
  return <Box>RejectionAnalytics Component</Box>;
};

export default RejectionAnalytics;
