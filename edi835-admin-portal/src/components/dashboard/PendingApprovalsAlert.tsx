import React from 'react';
import { Box } from '@mui/material';

interface PendingApprovalsAlertProps {
  [key: string]: any;
}

const PendingApprovalsAlert: React.FC<PendingApprovalsAlertProps> = (props) => {
  return <Box>PendingApprovalsAlert Component</Box>;
};

export default PendingApprovalsAlert;
